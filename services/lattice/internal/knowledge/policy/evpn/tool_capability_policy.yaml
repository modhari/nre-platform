tools:
  inspect_evpn_control_plane_state:
    required_capabilities:
      - bgp_evpn_peer_state

  inspect_vxlan_vni_oper_state:
    required_capabilities:
      - vxlan_vni_oper_state

  inspect_overlay_diagnostics_support:
    required_capabilities:
      - vxlan_vni_oper_state

  inspect_mac_mobility_state:
    required_capabilities:
      - evpn_mac_vrf_state
      - bgp_evpn_peer_state

  inspect_duplicate_mac_state:
    required_capabilities:
      - evpn_mac_vrf_state

  inspect_mac_ip_binding_state:
    required_capabilities:
      - evpn_mac_vrf_state

  inspect_arp_suppression_state:
    required_capabilities:
      - evpn_mac_vrf_state

  inspect_anycast_gateway_consistency:
    required_capabilities:
      - bgp_evpn_peer_state
      - evpn_vtep_config

  inspect_evpn_route_scope:
    required_capabilities:
      - bgp_evpn_peer_state
      - evpn_mac_vrf_state

  inspect_dci_route_propagation:
    required_capabilities:
      - bgp_evpn_peer_state
      - vxlan_vni_oper_state

  inspect_gateway_nexthop_behavior:
    required_capabilities:
      - bgp_evpn_peer_state
      - vxlan_vni_oper_state

  inspect_multihoming_model:
    required_capabilities:
      - bgp_evpn_peer_state
      - evpn_vtep_config

  inspect_ethernet_segment_state:
    required_capabilities:
      - bgp_evpn_peer_state
      - evpn_vtep_config

  inspect_vtep_reachability:
    required_capabilities:
      - evpn_vtep_config
      - vxlan_vni_oper_state

  inspect_type2_type5_route_preference:
    required_capabilities:
      - bgp_evpn_peer_state
      - evpn_mac_vrf_state

  inspect_vendor_specific_evpn_state:
    required_capabilities:
      - bgp_evpn_peer_state
  inspect_esi_df_election_state:
    required_capabilities:
      - bgp_evpn_peer_state
      - evpn_vtep_config

  inspect_vni_vlan_mapping:
    required_capabilities:
      - vxlan_vni_oper_state
      - vxlan_vni_config

  inspect_nve_interface_state:
    required_capabilities:
      - vxlan_vni_oper_state

  inspect_vrf_route_policy:
    required_capabilities:
      - bgp_evpn_peer_config
      - bgp_evpn_peer_state
